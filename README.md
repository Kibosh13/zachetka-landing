# Учебный советник — лендинг консультационных услуг по учебным работам

Одностраничный сайт на чистом HTML/CSS/JS. Без сборки и зависимостей — достаточно открыть `index.html`
или залить папку на любой хостинг (GitHub Pages, Netlify, Vercel, обычный shared-хостинг).

## Структура

```
index.html              вся разметка страницы
assets/css/styles.css   дизайн-система и стили
assets/css/fonts.css    подключение локального шрифта
assets/js/main.js       прайс, калькулятор, форма, анимации
assets/fonts/*.woff2    Manrope (variable 200–800), кириллица и латиница, 40 КБ
assets/img/*.png        логотип, иконки, превью для соцсетей
tools/                  скрипт пересборки графики логотипа
```

Шрифт лежит локально, а не подгружается с Google Fonts — сайт одинаково быстро открывается
у пользователей в России и не зависит от доступности внешнего CDN.

## Логотип

Исходник заказчика — `assets/img/logo-original.jpg`. Остальная графика собирается из него
скриптом `tools/make-logo-assets.py` (нужны `pillow numpy fonttools brotli`):

```bash
python3 tools/make-logo-assets.py
```

Скрипт вырезает белый фон и раскладывает логотип по местам: `logo-mark.png` — эмблема для
хедера, `logo.png` — полный лок-ап со слоганом для футера, `favicon.png` и
`apple-touch-icon.png` — иконки, `og.png` — превью 1200×630 для мессенджеров и соцсетей.
Если заказчик пришлёт логотип в векторе (SVG/AI/PDF) — лучше заменить растр на него.

Цвета логотипа продублированы в CSS-переменных `--logo-navy`, `--logo-teal`, `--logo-gold`.

## Что есть на странице

- Первый экран с ключевым обещанием, метриками и бегущей строкой услуг
- Шесть карточек направлений с ценой «от»
- Четыре шага процесса работы
- Прайс-лист с переключением категорий (на мобильных превращается в карточки)
- Калькулятор стоимости: тип работы × дисциплина × срочность + допуслуги
- Блок гарантий и условий оплаты
- FAQ-аккордеон
- Форма заявки с валидацией

## Реквизиты

В футере (блок `.f-legal`) указаны данные предпринимателя: ИП Подойникова Екатерина Александровна,
ИНН 519055358634, ОГРНИП 326510000026871, Мурманская область.
Взяты из публичных государственных источников. Банковские реквизиты на сайте не публикуются —
если они понадобятся для оплаты, их логичнее отправлять в переписке по конкретному заказу.

Почта для связи: `ekatpod89@gmail.com` — указана в блоке заявки, в футере и в `mailto:` из формы.

## Что ещё осталось заглушкой

| Что | Где искать |
| --- | --- |
| MAX `@sovetnik` | `index.html` — блок `.order-contacts`, футер |
| Адрес сайта в OG-тегах | `index.html` — `<meta property="og:url">` и `og:image` |

Адрес в OG-тегах сейчас указывает на GitHub Pages. Соцсети не понимают относительные пути,
поэтому при переезде на свой домен эти два тега нужно поправить руками.

## Прайс и калькулятор

Все цены лежат в одном месте — массиве `PRICE_DATA` в начале `assets/js/main.js`.
Таблица прайса и список в калькуляторе строятся из него, поэтому цену достаточно поменять один раз.

Поле `calc` у услуги отвечает за калькулятор:

```js
{ name: 'Курсовая работа (теоретическая)', volume: '25–35 стр.', term: '7–14 дней',
  price: '2 000 – 6 000 ₽', calc: { min: 2000, max: 6000, term: '7–14 дней' } }
```

`calc: null` — услуга видна в прайсе, но не участвует в калькуляторе (например, цены за страницу
или надбавки в процентах).

Коэффициенты калькулятора заданы в разметке, в `data-value` у кнопок:
техническая дисциплина `1.3`, ускоренный срок `1.5`, срочный `2`. Допуслуги — `data-add` в рублях.

## Форма заявки

Бэкенда нет: по кнопке «Отправить заявку» собирается письмо и открывается почтовый клиент.
Чтобы заявки приходили в CRM или Telegram, замените в `initForm()` блок с `window.location.href = mailto:`
на обычный запрос:

```js
await fetch('/api/lead', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ name: name.value, contact: contact.value, topic: topic.value })
});
```

## Локальный просмотр

```bash
python3 -m http.server 8080
```

Затем открыть http://localhost:8080

## Публикация

Основной адрес — https://uchebnyisovetnik.ru, хостинг Beget (аккаунт `ekatpo7j`),
папка сайта `~/uchebnyisovetnik.ru/public_html`. Обновление — заливка файлов по FTP
на `ekatpo7j.beget.tech` (лучше FTPS; Beget требует переиспользования TLS-сессии
на канале данных, обычный `FTP_TLS` из Python без доработки на нём падает).

Заливать нужно `index.html`, `.htaccess` и папку `assets`. Копия лежит на GitHub Pages —
там же исходники и история правок.

`.htaccess` уводит `www` и `http` на https основного домена и выставляет срок кэша
для статики. На техническом домене `ekatpo7j.beget.tech` лежит отдельный `.htaccess`
с 301-м редиректом на основной адрес, чтобы в поиске не было дубля сайта.

SSL — бесплатный Let's Encrypt, Beget выпустил его сам при подключении домена
и продлевает автоматически.
